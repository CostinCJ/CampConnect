{
  "name": "campconnect-seed-scripts",
  "private": true,
  "description": "One-shot Firestore seed scripts for CampConnect (not deployed).",
  "scripts": {
    "seed": "node seed_firestore.js",
    "wipe": "node seed_firestore.js --wipe"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
